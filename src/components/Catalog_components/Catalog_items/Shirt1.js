import { } from '../../../images/ProductImages';

// const itemType = products.find(item => item.id === 'Shirts');
// const product = itemType.imgArr.find(item => item.id === 'NikeMaizeShirt');

const Shirt1 = () => {
    console.log('rendered');
    return (
        <img src='' alt='' />
    );
};

export default Shirt1;
